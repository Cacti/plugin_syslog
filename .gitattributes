# Normalize all text files to LF line endings, regardless of platform.
* text=auto eol=lf

# Binary files must never be normalized or diffed as text.
*.mo binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
